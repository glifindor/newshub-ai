# 🚀 NewsHub AI - Быстрый старт

## Для тех, кто "ничего не понимает" 😊

Не переживайте! Следуйте этой инструкции шаг за шагом.

---

## 📋 Что нужно подготовить

### 1. Получить API ключи (бесплатно/платно):

#### a) OpenRouter (для AI-анализа)
1. Перейдите на https://openrouter.ai/
2. Зарегистрируйтесь
3. Перейдите в Settings → API Keys
4. Создайте новый ключ (будет выглядеть как `sk-or-v1-...`)
5. Пополните баланс на $5-10 для начала

#### b) Freepik API (для картинок)
1. Перейдите на https://www.freepik.com/api
2. Зарегистрируйтесь и получите API ключ
3. (Альтернатива: можно использовать бесплатный Unsplash API)

#### c) NewsAPI (для новостей)
1. Перейдите на https://newsapi.org/
2. Зарегистрируйтесь (бесплатный план: 100 запросов/день)
3. Скопируйте API ключ

#### d) Telegram Bot
1. Найдите в Telegram бота @BotFather
2. Отправьте команду `/newbot`
3. Следуйте инструкциям, дайте боту имя
4. Скопируйте токен (будет выглядеть как `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

#### e) Создайте Telegram-каналы
1. В Telegram создайте два канала:
   - @crypto_ainews (или своё название)
   - @kremlin_digest (или своё название)
2. Добавьте вашего бота в каналы как администратора

#### f) Узнайте свой Telegram ID
1. Найдите бота @userinfobot
2. Отправьте ему любое сообщение
3. Он ответит с вашим ID (число, например: 123456789)

---

## 💻 Установка на сервер (151.241.228.203)

### Шаг 1: Подключитесь к серверу

Откройте терминал (PowerShell на Windows) и выполните:

```powershell
ssh root@151.241.228.203
```

Введите пароль от сервера.

---

### Шаг 2: Установите необходимые программы

Скопируйте и вставьте эти команды по очереди:

```bash
# Обновление системы
apt update && apt upgrade -y

# Установка Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh

# Установка Docker Compose
apt install docker-compose -y

# Установка Git
apt install git -y

# Проверка установки
docker --version
docker-compose --version
git --version
```

Если всё установилось без ошибок, переходите дальше.

---

### Шаг 3: Скачайте проект

```bash
# Создание директории
mkdir -p /opt/newshub
cd /opt/newshub

# Клонирование проекта (ЗАМЕНИТЕ на ваш репозиторий!)
git clone https://github.com/glifindor/newsportal.git .

# Проверка структуры
ls -la
```

Должны появиться папки: `backend`, `frontend`, `nginx`, файлы `docker-compose.yml` и т.д.

---

### Шаг 4: Настройте переменные окружения

```bash
# Скопируйте шаблон
cp .env.example .env

# Откройте редактор
nano .env
```

**Замените эти строки на свои данные:**

```env
# Database (придумайте свой пароль!)
POSTGRES_PASSWORD=МойСуперСекретныйПароль123

# JWT (сгенерируйте случайную строку)
JWT_SECRET_KEY=ВставьтеСюдаСлучайнуюСтроку32СимволаМинимум

# OpenRouter
OPENROUTER_API_KEY=sk-or-v1-ваш_ключ_здесь

# Freepik
FREEPIK_API_KEY=ваш_ключ_freepik

# NewsAPI
NEWSAPI_KEY=ваш_ключ_newsapi

# Telegram
TELEGRAM_BOT_TOKEN=123456:ABCdefваш_токен_бота
TELEGRAM_CRYPTO_CHANNEL=@crypto_ainews
TELEGRAM_POLITICS_CHANNEL=@kremlin_digest
TELEGRAM_ADMIN_CHAT_ID=123456789
```

**Сохраните файл:**
- Нажмите `Ctrl + X`
- Нажмите `Y`
- Нажмите `Enter`

---

### Шаг 5: Сгенерируйте случайный ключ для JWT

```bash
# Выполните команду для генерации ключа
openssl rand -hex 32

# Скопируйте вывод (длинная строка букв и цифр)
# Откройте снова .env и вставьте в JWT_SECRET_KEY
nano .env
```

---

### Шаг 6: Запустите проект!

```bash
# Сборка и запуск всех сервисов
docker-compose up -d --build
```

Это займёт 5-10 минут (скачивание образов, сборка). Ждите...

**Проверка статуса:**

```bash
# Посмотреть запущенные контейнеры
docker-compose ps

# Все сервисы должны быть в состоянии "Up"
```

---

### Шаг 7: Создайте администратора

```bash
# Войдите в контейнер backend
docker-compose exec backend bash

# Создайте суперпользователя
python scripts/create_admin.py

# Введите данные:
# Username: admin
# Email: your@email.com
# Password: (придумайте пароль)

# Выйдите из контейнера
exit
```

---

### Шаг 8: Проверьте работу!

Откройте в браузере:

1. **Главный сайт:** http://151.241.228.203
2. **API документация:** http://151.241.228.203:8000/docs
3. **Админ панель:** http://151.241.228.203/admin
4. **Grafana (мониторинг):** http://151.241.228.203:3001
   - Логин: `admin`
   - Пароль: `admin` (смените при первом входе!)
5. **RabbitMQ:** http://151.241.228.203:15672
   - Логин: `guest`
   - Пароль: `guest`

---

## 🎉 Готово! Что дальше?

### 1. Добавьте источники новостей

Перейдите в админ-панель:
1. Войдите с созданным логином/паролем
2. Раздел "Источники новостей" → "Добавить источник"
3. Добавьте RSS-фиды:

**Для криптовалюты:**
- CoinDesk: `https://www.coindesk.com/arc/outboundfeeds/rss/`
- CoinTelegraph: `https://cointelegraph.com/rss`
- Bitcoin Magazine: `https://bitcoinmagazine.com/.rss/full/`

**Для политики:**
- TASS: `https://tass.ru/rss/v2.xml`
- РИА Новости: `https://ria.ru/export/rss2/archive/index.xml`
- Reuters: `https://www.reutersagency.com/feed/`

### 2. Запустите первый сбор новостей

```bash
# Вручную запустите сборщик (для теста)
docker-compose exec backend python -m app.tasks.collector
```

Через 1-2 минуты новости появятся в админ-панели!

### 3. Настройте автоматический постинг

В админ-панели:
- Раздел "Настройки"
- Включите "Автопостинг в Telegram"
- Установите "Интервал сбора": 15 минут

---

## 🔧 Полезные команды

### Просмотр логов:

```bash
# Все логи
docker-compose logs -f

# Только backend
docker-compose logs -f backend

# Только Celery (задачи)
docker-compose logs -f celery_worker
```

### Перезапуск сервисов:

```bash
# Перезапустить всё
docker-compose restart

# Перезапустить только backend
docker-compose restart backend
```

### Остановка проекта:

```bash
docker-compose down
```

### Обновление проекта:

```bash
# Скачать изменения из Git
git pull

# Пересобрать и перезапустить
docker-compose up -d --build
```

---

## 🆘 Проблемы и решения

### "Cannot connect to the Docker daemon"
```bash
# Запустите Docker
systemctl start docker

# Добавьте в автозагрузку
systemctl enable docker
```

### "Port 80 already in use"
```bash
# Найдите, что использует порт
lsof -i :80

# Остановите процесс (например, Apache)
systemctl stop apache2
```

### "Database connection failed"
```bash
# Проверьте статус PostgreSQL
docker-compose exec postgres pg_isready

# Посмотрите логи
docker-compose logs postgres
```

### "Telegram Bot не отвечает"
1. Проверьте правильность токена в `.env`
2. Убедитесь, что бот добавлен в каналы как админ
3. Отправьте боту в личку `/start`

---

## 📞 Нужна помощь?

1. Проверьте логи: `docker-compose logs -f`
2. Посмотрите ARCHITECTURE.md для деталей
3. Создайте Issue на GitHub
4. Напишите в Telegram админу

---

## 🔐 Безопасность

**ВАЖНО! После установки:**

1. **Измените пароли по умолчанию:**
   - Grafana (`admin`/`admin`)
   - RabbitMQ (`guest`/`guest`)
   - Ваш администратор в системе

2. **Настройте файрвол:**
```bash
# Установка UFW
apt install ufw -y

# Разрешить SSH, HTTP, HTTPS
ufw allow 22
ufw allow 80
ufw allow 443

# Включить
ufw enable
```

3. **Настройте SSL (HTTPS):**
```bash
# Установка Certbot
apt install certbot -y

# Получение сертификата (ЗАМЕНИТЕ домен!)
certbot certonly --standalone -d newshub.example.com

# Сертификаты будут в /etc/letsencrypt/live/
```

4. **Бэкапы:**
```bash
# Создайте cron-задачу для бэкапа БД
crontab -e

# Добавьте строку (бэкап каждый день в 3:00)
0 3 * * * docker-compose exec -T postgres pg_dump -U newsadmin newshub_db > /backups/newshub_$(date +\%Y\%m\%d).sql
```

---

## 📚 Дополнительные материалы

- [Документация FastAPI](https://fastapi.tiangolo.com/)
- [Документация Next.js](https://nextjs.org/docs)
- [Telegram Bot API](https://core.telegram.org/bots/api)
- [OpenRouter Docs](https://openrouter.ai/docs)

---

**Удачи с запуском! 🚀**
