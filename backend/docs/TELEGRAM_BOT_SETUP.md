# 🤖 Настройка Telegram Бота для NewsHub AI

## Пошаговая инструкция

### Шаг 1: Создание бота в BotFather

1. **Откройте Telegram** и найдите [@BotFather](https://t.me/BotFather)

2. **Отправьте команду:** `/newbot`

3. **Введите имя бота:**
   ```
   NewsHub AI Bot
   ```

4. **Введите username бота:**
   ```
   NewsHubAI_bot
   ```
   *(должен заканчиваться на `_bot`)*

5. **BotFather выдаст токен:**
   ```
   8286012057:AAG7YfZlvgij4aS-7Z9QzMBFfDhUsHphj9o
   ```
   
   ⚠️ **ВАЖНО:** Сохраните этот токен! Он понадобится для `.env` файла.

6. **Настройте описание бота (опционально):**
   ```
   /setdescription
   ```
   Введите:
   ```
   🤖 Автоматический бот для публикации новостей в каналы @crypto_ainews и @kremlin_digest
   
   Функции:
   • 📰 Сбор новостей из RSS и NewsAPI
   • 🤖 AI-анализ через GPT-4
   • 📤 Автопостинг в каналы
   • 🔍 Умные инсайты
   ```

7. **Отключите Privacy Mode** (чтобы бот мог читать сообщения в группах):
   ```
   /setprivacy
   → Выберите @NewsHubAI_bot
   → Выберите "Disable"
   ```

---

### Шаг 2: Создание Telegram каналов

#### 2.1 Создание канала для Крипто-новостей

1. **Откройте Telegram** → **Меню** → **Новый канал**

2. **Введите название:**
   ```
   Crypto AI News
   ```

3. **Введите описание:**
   ```
   🔐 Крипто-новости с AI-анализом
   
   🤖 Автоматический агрегатор новостей о криптовалютах, блокчейне и Web3.
   
   Источники: CoinTelegraph, CoinDesk, TechCrunch
   Технологии: GPT-4, FastAPI, Telegram Bot API
   ```

4. **Выберите тип:** `Публичный канал`

5. **Введите публичную ссылку:**
   ```
   crypto_ainews
   ```
   
   Полный адрес: `@crypto_ainews`

6. **Нажмите "Сохранить"**

#### 2.2 Создание канала для Политики

Повторите те же шаги:

- **Название:** `Kremlin Digest`
- **Описание:**
  ```
  🏛️ Политические новости с AI-анализом
  
  🤖 Автоматический агрегатор новостей о России и мировой политике.
  
  Источники: РИА Новости, TASS, BBC Russia
  ```
- **Публичная ссылка:** `kremlin_digest`
- **Адрес:** `@kremlin_digest`

---

### Шаг 3: Добавление бота в каналы как администратора

#### 3.1 Для канала @crypto_ainews

1. **Откройте канал** @crypto_ainews

2. **Нажмите на название канала** (вверху экрана)

3. **Перейдите в "Администраторы"** → **"Добавить администратора"**

4. **Найдите и выберите** @NewsHubAI_bot

5. **Выдайте права:**
   - ✅ Публиковать сообщения
   - ✅ Редактировать сообщения других администраторов
   - ❌ Добавлять новых администраторов (не нужно)
   - ❌ Удалять сообщения других пользователей (не нужно)
   - ❌ Банить пользователей (не нужно)
   - ❌ Приглашать пользователей по ссылке (не нужно)
   - ❌ Закреплять сообщения (не нужно)

6. **Нажмите "Сохранить"**

#### 3.2 Для канала @kremlin_digest

Повторите те же шаги для канала @kremlin_digest.

---

### Шаг 4: Получение Chat ID каналов

Chat ID каналов обычно начинаются с `-100`.

#### Метод 1: Через Bot API (рекомендуется)

1. **Отправьте любое сообщение** в канал @crypto_ainews (например, "Test")

2. **Откройте в браузере:**
   ```
   https://api.telegram.org/bot8286012057:AAG7YfZlvgij4aS-7Z9QzMBFfDhUsHphj9o/getUpdates
   ```
   
   *(замените токен на ваш)*

3. **Найдите в JSON ответе:**
   ```json
   {
     "chat": {
       "id": -1001234567890,
       "title": "Crypto AI News",
       "username": "crypto_ainews",
       "type": "channel"
     }
   }
   ```
   
   **Chat ID:** `-1001234567890`

4. **Повторите для @kremlin_digest**

#### Метод 2: Через специальный бот

1. **Добавьте бота** [@RawDataBot](https://t.me/RawDataBot) в канал

2. **Бот отправит JSON** с информацией о канале

3. **Найдите** `"id": -1001234567890`

4. **Удалите @RawDataBot** из канала

---

### Шаг 5: Получение вашего Admin Chat ID

Это ваш личный ID в Telegram (для уведомлений).

1. **Напишите боту** [@userinfobot](https://t.me/userinfobot)

2. **Отправьте любое сообщение**

3. **Бот ответит:**
   ```
   Id: 433868823
   First name: Ваше Имя
   Username: @ваш_username
   ```
   
   **Ваш Chat ID:** `433868823`

Альтернатива - напишите своему боту:

1. **Найдите в Telegram** @NewsHubAI_bot

2. **Отправьте** `/start`

3. **Откройте в браузере:**
   ```
   https://api.telegram.org/bot8286012057:AAG7YfZlvgij4aS-7Z9QzMBFfDhUsHphj9o/getUpdates
   ```

4. **Найдите ваш ID** в ответе:
   ```json
   {
     "message": {
       "from": {
         "id": 433868823,
         "first_name": "Ваше Имя"
       }
     }
   }
   ```

---

### Шаг 6: Настройка .env файла

Теперь добавьте все полученные данные в `.env`:

```bash
# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=8286012057:AAG7YfZlvgij4aS-7Z9QzMBFfDhUsHphj9o

# Для публичных каналов можно использовать @username
TELEGRAM_CRYPTO_CHANNEL=@crypto_ainews
TELEGRAM_POLITICS_CHANNEL=@kremlin_digest

# Или Chat ID (если каналы приватные)
# TELEGRAM_CRYPTO_CHANNEL=-1001234567890
# TELEGRAM_POLITICS_CHANNEL=-1009876543210

# Ваш личный Chat ID для уведомлений
TELEGRAM_ADMIN_CHAT_ID=433868823
```

---

### Шаг 7: Тестирование бота

#### 7.1 Простой тест отправки

Создайте файл `backend/scripts/test_telegram_quick.py`:

```python
"""Быстрый тест Telegram бота"""
import asyncio
from telegram import Bot

# Ваши данные
BOT_TOKEN = "8286012057:AAG7YfZlvgij4aS-7Z9QzMBFfDhUsHphj9o"
CRYPTO_CHANNEL = "@crypto_ainews"
POLITICS_CHANNEL = "@kremlin_digest"
ADMIN_CHAT_ID = 433868823


async def test_bot():
    """Тест бота"""
    bot = Bot(token=BOT_TOKEN)
    
    print("🤖 Тестирование Telegram Bot...")
    
    # 1. Проверка бота
    try:
        me = await bot.get_me()
        print(f"✅ Бот подключен!")
        print(f"   Имя: {me.first_name}")
        print(f"   Username: @{me.username}")
        print(f"   ID: {me.id}")
    except Exception as e:
        print(f"❌ Ошибка подключения: {e}")
        return
    
    # 2. Отправка тестового сообщения админу
    try:
        await bot.send_message(
            chat_id=ADMIN_CHAT_ID,
            text="✅ Тестовое сообщение от NewsHub AI Bot!\n\nБот работает корректно.",
        )
        print(f"✅ Тестовое сообщение отправлено админу (ID: {ADMIN_CHAT_ID})")
    except Exception as e:
        print(f"❌ Ошибка отправки админу: {e}")
    
    # 3. Отправка в крипто-канал
    try:
        test_message = """
🔐 <b>Bitcoin достиг исторического максимума!</b>

📝 Криптовалюта Bitcoin впервые в истории преодолела отметку в $100,000. Аналитики связывают рост с массовым принятием институциональными инвесторами.

🔍 <b>AI-инсайт:</b>
• Институциональные инвестиции выросли на 300%
• Ожидается дальнейший рост до $150,000
• Регуляторная ясность в США стимулирует рынок

🔗 <a href='https://example.com'>Читать подробнее</a>

#Bitcoin #Crypto #ATH
"""
        
        msg = await bot.send_message(
            chat_id=CRYPTO_CHANNEL,
            text=test_message,
            parse_mode="HTML",
        )
        print(f"✅ Сообщение опубликовано в {CRYPTO_CHANNEL}")
        print(f"   Message ID: {msg.message_id}")
        print(f"   Ссылка: https://t.me/{CRYPTO_CHANNEL.lstrip('@')}/{msg.message_id}")
    except Exception as e:
        print(f"❌ Ошибка отправки в {CRYPTO_CHANNEL}: {e}")
        print(f"   Проверьте, что бот добавлен как администратор!")
    
    # 4. Отправка в политический канал
    try:
        test_message = """
🏛️ <b>Новый саммит G20 в Москве</b>

📝 Лидеры стран G20 соберутся в Москве для обсуждения глобальных экономических вопросов и климатических изменений.

🔍 <b>AI-инсайт:</b>
• Ожидается подписание ключевых соглашений
• Россия предложит новую климатическую инициативу
• Фокус на цифровой экономике

🔗 <a href='https://example.com'>Читать подробнее</a>

#G20 #Политика #Москва
"""
        
        msg = await bot.send_message(
            chat_id=POLITICS_CHANNEL,
            text=test_message,
            parse_mode="HTML",
        )
        print(f"✅ Сообщение опубликовано в {POLITICS_CHANNEL}")
        print(f"   Message ID: {msg.message_id}")
    except Exception as e:
        print(f"❌ Ошибка отправки в {POLITICS_CHANNEL}: {e}")
        print(f"   Проверьте, что бот добавлен как администратор!")
    
    print("\n✅ Тестирование завершено!")


if __name__ == "__main__":
    asyncio.run(test_bot())
```

Запустите тест:

```powershell
cd backend
python scripts/test_telegram_quick.py
```

**Ожидаемый результат:**
```
🤖 Тестирование Telegram Bot...
✅ Бот подключен!
   Имя: NewsHub AI Bot
   Username: @NewsHubAI_bot
   ID: 8286012057
✅ Тестовое сообщение отправлено админу (ID: 433868823)
✅ Сообщение опубликовано в @crypto_ainews
   Message ID: 123
   Ссылка: https://t.me/crypto_ainews/123
✅ Сообщение опубликовано в @kremlin_digest
   Message ID: 456
✅ Тестирование завершено!
```

---

### Шаг 8: Проверка через backend API

Запустите backend:

```powershell
cd backend
uvicorn app.main:app --reload
```

Откройте Swagger UI: http://localhost:8000/docs

#### Тест 1: Отправка тестовой новости

1. Найдите endpoint: `POST /api/v1/telegram/test`

2. Нажмите **"Try it out"**

3. Введите JSON:
   ```json
   {
     "channel": "crypto",
     "title": "Test News",
     "content": "This is a test message from NewsHub AI!"
   }
   ```

4. Нажмите **"Execute"**

5. **Проверьте канал @crypto_ainews** - должно появиться сообщение!

#### Тест 2: Полный pipeline

```bash
# 1. Собрать новости
curl -X POST "http://localhost:8000/api/v1/pipeline/collect?channel=crypto"

# 2. Проанализировать
curl -X POST "http://localhost:8000/api/v1/pipeline/analyze?limit=3"

# 3. Опубликовать
curl -X POST "http://localhost:8000/api/v1/pipeline/post?limit=1"
```

**Проверьте каналы** - новости должны появиться!

---

## 🔧 Troubleshooting

### Ошибка: "Unauthorized"

**Причина:** Неверный токен бота

**Решение:**
1. Проверьте `TELEGRAM_BOT_TOKEN` в `.env`
2. Создайте нового бота через @BotFather
3. Скопируйте новый токен

---

### Ошибка: "Bad Request: chat not found"

**Причина:** Неверный Chat ID канала

**Решение:**
1. Проверьте, что канал существует
2. Получите Chat ID заново через `/getUpdates`
3. Обновите `.env`

---

### Ошибка: "Bad Request: CHAT_WRITE_FORBIDDEN"

**Причина:** Бот не добавлен как администратор канала

**Решение:**
1. Откройте настройки канала
2. Администраторы → Добавить администратора
3. Найдите @NewsHubAI_bot
4. Выдайте права "Публиковать сообщения"

---

### Ошибка: "Too Many Requests: retry after X"

**Причина:** Превышен rate limit Telegram (20 msg/min)

**Решение:**
- Это нормально! Бот автоматически подождёт X секунд
- Rate limiter в коде обрабатывает это
- Можно увеличить интервал в `app/services/telegram_poster.py`:
  ```python
  self.rate_limiter = RateLimiter(max_messages=15, time_window=60)
  ```

---

### Ошибка: "NetworkError" или "TimedOut"

**Причина:** Проблемы с сетью или блокировка Telegram

**Решение:**
1. Проверьте интернет-соединение
2. Если Telegram заблокирован, используйте VPN/прокси:
   ```python
   # В app/services/telegram_poster.py
   from telegram.request import HTTPXRequest
   
   request = HTTPXRequest(
       proxy_url="http://your-proxy:8080"
   )
   self.bot = Bot(token=settings.TELEGRAM_BOT_TOKEN, request=request)
   ```

---

## 📊 Мониторинг

### Просмотр логов

```powershell
# Реал-тайм логи
docker-compose logs -f backend | grep "TelegramPoster"

# Поиск ошибок
docker-compose logs backend | grep "Error posting"

# Статистика отправок
docker-compose logs backend | grep "News posted successfully"
```

### Просмотр статистики в БД

```sql
-- Количество опубликованных новостей
SELECT 
    telegram_channel,
    COUNT(*) as total_published,
    DATE(published_at) as date
FROM news_items
WHERE status = 'published'
GROUP BY telegram_channel, DATE(published_at)
ORDER BY date DESC;

-- Последние публикации
SELECT 
    title,
    telegram_channel,
    telegram_message_id,
    published_at
FROM news_items
WHERE status = 'published'
ORDER BY published_at DESC
LIMIT 10;
```

---

## 🎯 Полезные ссылки

- **Telegram Bot API Docs:** https://core.telegram.org/bots/api
- **python-telegram-bot Docs:** https://docs.python-telegram-bot.org/
- **BotFather:** https://t.me/BotFather
- **Get Updates API:** https://api.telegram.org/bot{TOKEN}/getUpdates
- **Ваши каналы:**
  - @crypto_ainews: https://t.me/crypto_ainews
  - @kremlin_digest: https://t.me/kremlin_digest

---

## ✅ Чеклист готовности

- [ ] Бот создан через @BotFather
- [ ] Токен скопирован в `.env`
- [ ] Созданы оба канала (@crypto_ainews, @kremlin_digest)
- [ ] Бот добавлен как администратор в оба канала
- [ ] Получены Chat ID каналов
- [ ] Получен Admin Chat ID
- [ ] Всё добавлено в `.env`
- [ ] Запущен `test_telegram_quick.py` - всё ✅
- [ ] Тестовая новость опубликована через API
- [ ] Админ получает уведомления

**Если все пункты отмечены - бот готов к работе! 🎉**

---

## 🚀 Дальнейшие улучшения

### 1. Добавить кнопки под постами

```python
from telegram import InlineKeyboardButton, InlineKeyboardMarkup

keyboard = InlineKeyboardMarkup([
    [InlineKeyboardButton("📰 Читать на сайте", url=news.url)],
    [InlineKeyboardButton("💬 Обсудить", url="https://t.me/newsclub")],
])

await bot.send_message(
    chat_id=channel,
    text=message_text,
    reply_markup=keyboard,
)
```

### 2. Добавить реакции

```python
# Установить реакцию на пост
await bot.set_message_reaction(
    chat_id=channel,
    message_id=message.message_id,
    reaction=[{"type": "emoji", "emoji": "🔥"}]
)
```

### 3. Отложенная публикация

```python
from datetime import datetime, timedelta

# Запланировать публикацию на завтра в 10:00
schedule_time = datetime.now() + timedelta(days=1)
schedule_time = schedule_time.replace(hour=10, minute=0)

# Сохранить в БД с scheduled_at
news.scheduled_at = schedule_time
```

### 4. Analytics

```python
# Получить статистику поста
async def get_post_stats(channel: str, message_id: int):
    # Через Telegram Bot API нельзя, но можно интегрировать Telethon
    from telethon import TelegramClient
    
    client = TelegramClient('session', api_id, api_hash)
    message = await client.get_messages(channel, ids=message_id)
    
    return {
        'views': message.views,
        'forwards': message.forwards,
    }
```

---

**Готово! Ваш бот полностью настроен и готов к автоматической публикации новостей! 🎉**
